package launcher;

import controller.Controller;
import constant.Constant; 

import model.*;
import shed.Tools;

import java.util.LinkedList;

public class Launcher {
	public static void main(String[] args) {
		Controller c = new Controller();
		// Boundary d1 = new Boundary(new Point(3, 2), new Point(3, -2));
		// Boundary d2 = new Boundary(new Point(-3, 2), new Point(-1, 3));
		// Boundary d3 = new Boundary(new Point(-3, -2), new Point(-1, -2));
		// Boundary d4 = new Boundary(new Point(1, 3), new Point(5, 1));

		// Boundary d100 = new Boundary(new Point(0.0000001, 10), new Point(0.0000001, -10));

		// LinkedList<Boundary> bounds = new LinkedList<Boundary>();
		// bounds.add(d1);
		// bounds.add(d2);
		// bounds.add(d3);
		// bounds.add(d4);
		// bounds.add(d100);


		// Ray r = new Ray(new Point(0, 0), new Point(4, 0), d1);
		// r.updateDirection(bounds);		
	}
}